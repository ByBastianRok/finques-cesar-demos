{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "cmd.exe",
      "args": ["/c", "npx", "-y", "@playwright/mcp@latest", "--browser", "msedge", "--output-dir", "%USERPROFILE%\\playwright-mcp-out"]
    }
  }
}
